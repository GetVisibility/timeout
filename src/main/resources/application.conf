my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 1h
  }
}

akka {
  http {
    server {
      request-timeout = 1h
      idle-timeout = 1h
    }
  }
}
